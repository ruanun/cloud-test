server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #注册中心地址
      config:
        server-addr: localhost:8848 #nacos作为配置中心
        file-extension: yaml

